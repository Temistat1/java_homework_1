// Define two strings to compare
let string1 = "Hello";
let string2 = "Hello"; // Change this to test different scenarios

// Conditional statement to check if the strings are equal
if (string1 === string2) {
    alert("The strings are equal!");
} else {
    alert("The strings are not equal.");
}

